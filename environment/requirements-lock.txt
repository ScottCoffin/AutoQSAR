# AutoQSAR — pinned Linux/CUDA container lockfile
#
# Target: Python 3.11 / CUDA 12.1 / Ubuntu 22.04
# Base image: nvidia/cuda:12.1.1-cudnn8-runtime-ubuntu22.04
#
# CUDA VERSION NOTE: PyTorch wheels target CUDA 12.1. The host driver must
# support CUDA >= 12.1. As of mid-2026 ACCESS clusters running A100/V100/H100
# nodes carry driver 535+ which supports CUDA 12.2, so 12.1 wheels load fine.
#
# Windows-only packages (pywin32, pywinpty, win32-setctime) are EXCLUDED.
# Jupyter/notebook packages are EXCLUDED (not needed in batch HPC mode).
# Sphinx/docs packages are EXCLUDED.
#
# Install order matters for torch+cu121: install torch family first from the
# PyTorch index, then the rest from PyPI.

# ── PyTorch ecosystem (CUDA 12.1) ──────────────────────────────────────────
# Extra index: https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
torchdata==0.9.0
lightning==2.6.1
lightning-utilities==0.15.3
pytorch-lightning==2.6.1

# ── Graph neural networks ───────────────────────────────────────────────────
# DGL cu121 wheels from https://data.dgl.ai/wheels/torch-2.5/cu121/
dgl==2.2.1
dgllife==0.3.2

# ── ADMET / cheminformatics core ────────────────────────────────────────────
chemprop==2.2.3
rdkit==2026.3.1
descriptastorus==2.8.0
mhfp==1.9.6
selfies==2.2.0
datamol==0.12.5
molfeat==0.10.1

# ── Foundation models ────────────────────────────────────────────────────────
tabpfn==7.1.1
tabpfn-client==0.2.8
tabpfn-common-utils==0.2.20
unimol-tools==0.1.5

# ── Gradient boosting ────────────────────────────────────────────────────────
# NOTE: catboost and xgboost run CPU-only on datasets where no GPU winner was
# recorded; their GPU task-tree accelerators activate automatically when CUDA
# is available. lightgbm was missing from the original requirements files but
# IS imported in the codebase (line 284 of run_autoqsar_ga_benchmarks.py);
# pinned here to restore LightGBM model family.
catboost==1.2.10
xgboost==3.2.0
lightgbm==4.5.0

# ── Scientific Python ─────────────────────────────────────────────────────────
numpy==2.2.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.6.1
scikit-optimize==0.10.2
statsmodels==0.14.6
pyarrow==23.0.1
joblib==1.5.3
threadpoolctl==3.6.0
patsy==1.0.2

# ── AutoQSAR-specific / CFA / ML utilities ───────────────────────────────────
cfanalysis==0.1.13
forestci==0.7
shap==0.51.0
lime==0.2.0.1
kneed==0.8.6
hyperopt==0.2.7
astartes==1.3.3
madml==2.6.7
mastml==3.2.5

# ── Deep learning utilities ──────────────────────────────────────────────────
einops==0.8.2
lmdb==2.2.0
numba==0.65.0
llvmlite==0.47.0
networkx==3.3
sympy==1.14.0
mpmath==1.3.0

# ── TDC / dataset loaders ────────────────────────────────────────────────────
# PyTDC is bundled as a vendored source tarball (.cache/pytdc-1.1.15.tar.gz).
# Install from that tarball inside the container; omit from this lockfile.
# Other dataset infrastructure:
mordredcommunity==2.0.7
matminer==0.10.1
pymatgen==2026.3.23
pymatgen-core==2026.4.16
monty==2026.2.18
spglib==2.7.0

# ── Web / API / auth ─────────────────────────────────────────────────────────
requests==2.33.1
urllib3==2.6.3
certifi==2026.2.25
charset-normalizer==3.4.7
idna==3.11
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
huggingface-hub==1.10.1
filelock==3.25.2
fsspec==2026.3.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm==4.67.3
rich==14.0.0
loguru==0.7.3
pyyaml==6.0.3
ruamel-yaml==0.19.1
omegaconf==2.3.0
hydra-core==1.3.2
click==8.3.2
typer==0.24.1
configargparse==1.7.5
python-dotenv==1.2.2
psutil==7.2.2
packaging==26.0
setuptools==70.2.0
six==1.17.0
typing-extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0
pydantic==2.13.1
pydantic-core==2.46.1
pydantic-settings==2.14.0

# ── Visualization (minimal — for summary CSVs only) ──────────────────────────
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.7.0
pillow==12.2.0

# ── Misc scientific ──────────────────────────────────────────────────────────
openpyxl==3.1.5
pyarrow==23.0.1
xxhash==3.6.0
tabulate==0.10.0
uncertainties==3.2.3
umap-learn==0.5.12
pynndescent==0.6.0
scikeras==0.13.0
transfernet==0.4.2
aimsim-core==2.2.3
pmapper==1.1.3
pykan==0.0.5
mdf-forge==0.8.0
mdf-toolbox==0.7.1
fair-research-login==0.3.1
globus-sdk==3.65.0
